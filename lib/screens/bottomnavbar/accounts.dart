import 'package:flutter/material.dart';

class UseAccounts extends StatelessWidget {
  const UseAccounts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'H E L L O - A C C O U N T S',
        style: TextStyle(
          fontSize: 40,
          color: Colors.green,
        ),
      ),
    );
  }
}
