import 'package:flutter/material.dart';

class UseSettings extends StatelessWidget {
  const UseSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'H E L L O - S E T T I N G S',
        style: TextStyle(
          fontSize: 40,
          color: Colors.red,
        ),
      ),
    );
  }
}
